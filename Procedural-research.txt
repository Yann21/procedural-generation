Comptabilite GIMP, generation de map avec les Tiles_80x80.png
Correct 2-tile water crossing
2-8 / 1-9

3t**2 -2t**3

Perlin noise
Simplex noise

Voronoi diagram

Fortune's algorithm

river select elevated tiles follow ?steepest gradient?

if gradient is below a certain threshold: lake

biome.class > main.worldgen >> Room variable

add exponential in formatColorValue

implement fractal noise for trees

elevation octaves

from opensimplex import OpenSimplex
gen = OpenSimplex()
def noise(nx, ny):
    # Rescale from -1.0:+1.0 to 0.0:1.0
    return gen.noise2d(nx, ny) / 2.0 + 0.5

for y in range(height):
    for x in range(width):
        nx = x/width - 0.5
        ny = y/height - 0.5
        value[y][x] = noise(nx, ny)